export interface ThemeType {
  colors: {
    header: string;
    topHeader: string;
    body: string;
    footer: string;
    cart: string;
    card: string;
    buttons: string;
    text: string;
    icons: string;
  };
  mobile: string;
}
